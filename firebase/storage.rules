rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Bloqueia acesso anônimo; apenas autenticados podem ler/escrever seus arquivos
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
